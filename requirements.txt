# SWAM Project - Requirements
# Dipendenze Python per l'analisi multi-linguaggio del dataset Rosetta Code

# Dataset e Data Processing
datasets>=2.14.0          # Hugging Face datasets library per caricare rosetta-code
pandas>=2.0.0             # Data manipulation e analisi del dataset
numpy>=1.24.0             # Operazioni numeriche (dipendenza di pandas)

# Progress Bars e UI
tqdm>=4.65.0              # Progress bars per monitorare esecuzione

# Utilities
colorama>=0.4.6           # Supporto colori ANSI cross-platform (opzionale ma consigliato)

# Note:
# - subprocess, os, json, re, logging, datetime, time sono moduli built-in Python
# - Non serve installare nulla per questi moduli
